# Notebooks

Use numbered notebooks only for exploration after validated parquet inputs exist. Production logic
belongs in `src/gridflex`; notebooks must record configuration and data checksums and must not contain
committed credentials or fabricated demonstration data.
